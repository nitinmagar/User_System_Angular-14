# user management system is an application for adding updating and deleting user

use command to run application:

install dependencies:
npm install

run server:
node server.js

run application:
ng serve --o
